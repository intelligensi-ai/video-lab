import React,{useState}from'react';import{createRoot}from'react-dom/client';import{BrowserRouter,Link,Route,Routes,useNavigate,useParams}from'react-router-dom';import{QueryClient,QueryClientProvider,useQuery,useMutation}from'@tanstack/react-query';import type{Generation,RuntimeStatus,CreditWallet,Me}from'@video-lab/contracts';import'./style.css';
const API=import.meta.env.VITE_API_BASE_URL??'/api';const token=()=>localStorage.getItem('vl_token')||'demo-user';async function api<T>(path:string,init:RequestInit={}){const r=await fetch(`${API}${path}`,{...init,headers:{'content-type':'application/json',authorization:`Bearer ${token()}`,...init.headers}});if(!r.ok)throw new Error((await r.json()).detail??r.statusText);return r.json() as Promise<T>}
function Shell(){return <><nav><Link to="/">Video Lab</Link><Link to="/studio">Studio</Link><Link to="/gallery">Gallery</Link><Link to="/account">Account</Link><Link to="/admin">Admin</Link></nav><Routes><Route path="/" element={<Landing/>}/><Route path="/studio" element={<Studio/>}/><Route path="/gallery" element={<Gallery/>}/><Route path="/generations/:id" element={<Detail/>}/><Route path="/account" element={<Account/>}/><Route path="/admin" element={<Admin/>}/></Routes></>}
function Landing(){return <main className="hero"><div className="badge">Intelligensi.ai Showcase Trial</div><h1>Cinematic AI video generation, protected by credits and a private GPU queue.</h1><p>Scan, sign in, craft a prompt, and generate polished clips through the LTX Sulphur runtime without exposing infrastructure.</p><div className="prompts"><span>Neon rain over a glass city, anamorphic lens</span><span>A desert observatory opening beneath the stars</span><span>Macro crystal forest blooming in slow motion</span></div><Link className="button" to="/studio">Start creating</Link><footer><a>Privacy</a><a>Terms</a><small>Trial credits are limited and non-transferable.</small></footer></main>}
function Studio(){const nav=useNavigate();const credits=useQuery({queryKey:['credits'],queryFn:()=>api<CreditWallet>('/v1/credits')});const runtime=useQuery({queryKey:['runtime'],queryFn:()=>api<RuntimeStatus>('/v1/runtime/status')});const [prompt,setPrompt]=useState('A cinematic reveal of an intelligent glass monolith floating over an ocean at sunrise');const [quality,setQuality]=useState<'draft'|'standard'|'high'>('standard');const create=useMutation({mutationFn:()=>api<Generation>('/v1/generations',{method:'POST',headers:{'Idempotency-Key':crypto.randomUUID()},body:JSON.stringify({prompt,settings:{aspectRatio:'16:9',durationSeconds:4,quality}})}),onSuccess:g=>nav(`/generations/${g.id}`)});return <main><h1>Studio</h1><section className="panel"><textarea value={prompt} maxLength={1200} onChange={e=>setPrompt(e.target.value)}/><p>{prompt.length}/1200 characters</p><div className="chips">{['Volumetric light','Handheld documentary','Epic drone shot'].map(p=><button onClick={()=>setPrompt(prompt+' '+p)}>{p}</button>)}</div><label>Quality <select value={quality} onChange={e=>setQuality(e.target.value as typeof quality)}><option>draft</option><option>standard</option><option>high</option></select></label><div className="drop">Optional start, end, or reference image upload target is available through the API.</div><p>Credits: {credits.data?.available ?? '…'} available / {credits.data?.reserved ?? 0} reserved</p><p>Runtime: {runtime.data?.status} · Queue depth {runtime.data?.queueDepth}</p>{create.error&&<p className="error">{create.error.message}</p>}<button className="button" disabled={create.isPending} onClick={()=>create.mutate()}>{create.isPending?'Submitting…':'Generate video'}</button></section></main>}
function Gallery(){const q=useQuery({queryKey:['gallery'],queryFn:()=>api<{items:Generation[]}>('/v1/gallery')});return <main><h1>Personal Gallery</h1><div className="grid">{q.data?.items.length? q.data.items.map(g=><article className="card"><div className="thumb">{g.status}</div><h3>{g.prompt}</h3><p>{new Date(g.createdAt).toLocaleString()}</p><Link to={`/generations/${g.id}`}>Open details</Link></article>):<p className="empty">No generations yet. Create your first cinematic clip.</p>}</div></main>}
function Detail(){const{id}=useParams();const q=useQuery({queryKey:['gen',id],queryFn:()=>api<Generation>(`/v1/generations/${id}`),refetchInterval:1500});const proc=useMutation({mutationFn:()=>api('/v1/dev/process-one',{method:'POST'}),onSuccess:()=>q.refetch()});const cancel=useMutation({mutationFn:()=>api<Generation>(`/v1/generations/${id}/cancel`,{method:'POST'}),onSuccess:()=>q.refetch()});const g=q.data;return <main>{g&&<><h1>Generation</h1><section className="panel"><div className="thumb big">{g.status}</div><p>{g.prompt}</p><p>Cost: {g.creditCost} credits · Created {new Date(g.createdAt).toLocaleString()}</p>{g.safeErrorMessage&&<p className="error">{g.safeErrorMessage}</p>}{g.output?.downloadUrl&&<a className="button" href={g.output.downloadUrl}>Download</a>}<button onClick={()=>navigator.clipboard.writeText(g.prompt)}>Copy prompt</button><Link to="/studio">Create variation</Link>{!['completed','failed','cancelled'].includes(g.status)&&<button onClick={()=>cancel.mutate()}>Cancel</button>}<button onClick={()=>proc.mutate()}>Run mock worker</button></section></>}</main>}
function Account(){const me=useQuery({queryKey:['me'],queryFn:()=>api<Me>('/v1/me')});const cr=useQuery({queryKey:['credits'],queryFn:()=>api<CreditWallet>('/v1/credits')});return <main><h1>Account</h1><section className="panel"><p>{me.data?.email}</p><p>Roles: {me.data?.roles.join(', ')||'creator'}</p><p>Terms: {me.data?.termsVersion}</p><p>Trial granted: {me.data?.trialGrantedAt}</p><p>Available {cr.data?.available}; reserved {cr.data?.reserved}; spent {cr.data?.spent}</p><button onClick={()=>{localStorage.removeItem('vl_token');location.href='/'}}>Sign out</button><p>Data deletion request: contact operations; functional workflow is prepared for production support.</p><button disabled>Credit packs coming soon</button></section></main>}
function Admin(){const r=useQuery({queryKey:['runtime'],queryFn:()=>api<RuntimeStatus>('/v1/runtime/status')});const call=(p:string)=>api<RuntimeStatus>(p,{method:'POST'}).then(()=>r.refetch());return <main><h1>Admin Console</h1><section className="panel"><pre>{JSON.stringify(r.data,null,2)}</pre><button onClick={()=>call('/v1/admin/runtime/pause')}>Pause submissions</button><button onClick={()=>call('/v1/admin/runtime/resume')}>Resume</button><button onClick={()=>call('/v1/admin/runtime/stop')}>Kill switch</button><p>Use admin-token locally to exercise backend-enforced admin claims.</p></section></main>}
createRoot(document.getElementById('root')!).render(<QueryClientProvider client={new QueryClient()}><BrowserRouter><Shell/></BrowserRouter></QueryClientProvider>);
