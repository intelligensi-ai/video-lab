# Deployment
CI validates lint, typecheck, contract lint, tests, and builds. Deploy manually to Firebase Hosting/Functions after configuring secrets and project ID. Use GitHub environments for production secrets; do not auto-deploy every branch.
