# OpenAPI workflow
Edit `contracts/video-lab.openapi.yaml` first. Run `pnpm openapi:lint`, `pnpm openapi:generate`, `pnpm contract:test`, and then update route handlers/tests. Public state changes require contract and contract-test updates.
