# LongForm handover: start here

This directory is the canonical entry point for handing the accepted LongForm
creator workflow to John.

## Read in this order

1. [`longform-creator-workflow-to-john.md`](longform-creator-workflow-to-john.md)
   is the factual technical reference: architecture, contracts, tested
   revisions, persistence and ownership invariants, environment-variable names,
   accepted behaviour, remaining limits and John's responsibility boundary.
2. [`john-production-platform-goal.md`](john-production-platform-goal.md) is the
   implementation-ready `/goal` for John's Firebase, distributed rate limiting,
   entitlement, financial, storage, monitoring and rollout work.
3. Deploy Studio's
   `docs/runtime-validation/longform-gate-cwa8r3-acceptance-2026-08-12.md`
   contains the detailed paid A10 evidence, media hashes, timings, cost and
   provider-cleanup record.
4. The authoritative API contracts are
   `contracts/video-lab.openapi.yaml` and Deploy Studio's
   `docs/intelligensi-runtime-api.openapi.yaml`.

## Accepted technical baseline

- LongForm image: A10 immutable digest
  `sha256:e986a7c7bcb480ee7af065c459725ce1224cea959de825ded26727fbb4c13e14`.
- LongForm source: `8ecfe7bf33b975d6bc677e8c5decaafe39e1a98b`.
- Deploy Studio paid-test revision:
  `639e86adb03960172b6e5a6eb6ecf4a2fd1e37ad`.
- Deploy Studio handover/evidence revision before this index:
  `dffbd344ab3d5db02ec9abd0d61b2b018bfec462`.
- Video Lab paid-test revision before this index:
  `239cc67c6cb9dcf092d9d7d2c97e53079f476f11`.
- Workflow schema: `ltx23-reference-temporal-v4`.
- Instruction bundle hash:
  `c1887ef29d2314e0ed3dc43786b55a7a6be63937395aeb8e3fad394520dd6301`.
- Production runtime pin: unchanged; A10 has not been promoted.

CWA8R3 directly proved exact two-scene Gemma planning, targeted regeneration,
four owner-scoped anchor frames, independent first/last replacement, successful
version preservation, two-user fairness and isolation, cancellation, retry,
completed-job idempotency, one real anchored LTX clip, silent output,
persistence, stale signalling, restart recovery with identical hashes and
same-origin private media delivery. The owned paid worker was terminated and
Lambda independently reported zero active instances.

## Remaining work

John owns the production-platform work in the `/goal`. The remaining narrow UI
evidence is an advanced-desktop and mobile staging replay plus the complete
browser-origin audit. It can use the durable accepted project/media fixture and
does not require new Gemma, Z-Image or LTX inference unless a relevant runtime,
gateway, media or generation-payload boundary changes.

Do not infer production approval from this handover. Runtime promotion,
production Firebase deployment, DNS, payments and public launch require their
own explicit approvals and staging evidence.
