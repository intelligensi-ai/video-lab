# Firebase setup
Create a Firebase project, copy `.firebaserc.example` to `.firebaserc`, configure web app environment variables, and deploy rules/indexes with Firebase CLI. Emulators are configured in `firebase.json` for Auth, Functions, Firestore, Storage, and Emulator UI. Do not commit project credentials.
