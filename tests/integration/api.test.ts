import { describe, expect, it } from 'vitest';
import request from 'supertest';
import { app, processOne } from '../../apps/api/src/index.js';
describe('api integration',()=>{const auth={authorization:'Bearer testuser'};it('grants trial once and returns me',async()=>{const a=await request(app).get('/v1/me').set(auth).expect(200); const b=await request(app).get('/v1/credits').set(auth).expect(200); expect(a.body.uid).toBe('testuser'); expect(b.body.available).toBeGreaterThan(0);});it('accepts Firebase Hosting /api-prefixed routes and permits authenticated runtime connection testing',async()=>{const status=await request(app).get('/api/v1/runtime/status').set(auth).expect(200); expect(status.body.provider).toBeTruthy(); const invalid=await request(app).post('/api/v1/admin/runtime/connect').set(auth).send({lambdaIp:'localhost'}).expect(400); expect(invalid.body.code).toBe('invalid_runtime_endpoint');});it('submits idempotently, processes, and lists private gallery',async()=>{const body={prompt:'A cinematic ocean sunrise with glass monolith',settings:{aspectRatio:'16:9',durationSeconds:4,quality:'draft'}}; const a=await request(app).post('/v1/generations').set(auth).set('Idempotency-Key','same-key-123').send(body).expect(201); const b=await request(app).post('/v1/generations').set(auth).set('Idempotency-Key','same-key-123').send(body).expect(200); expect(b.body.id).toBe(a.body.id); await processOne('test'); const g=await request(app).get(`/v1/generations/${a.body.id}`).set(auth).expect(200); expect(g.body.status).toBe('completed'); const gal=await request(app).get('/v1/gallery').set(auth).expect(200); expect(gal.body.items.some((x:{id:string})=>x.id===a.body.id)).toBe(true);});it('keeps pause and kill-switch controls administrator-only',async()=>{await request(app).post('/v1/admin/runtime/pause').set(auth).expect(403); await request(app).post('/v1/admin/runtime/pause').set('authorization','Bearer admin-token').expect(200); await request(app).post('/v1/admin/runtime/resume').set('authorization','Bearer admin-token').expect(200);});it('validates asset ownership',async()=>{const up=await request(app).post('/v1/assets/upload-url').set({authorization:'Bearer owner'}).send({fileName:'a.png',contentType:'image/png',sizeBytes:100,purpose:'reference'}).expect(201); await request(app).post('/v1/generations').set({authorization:'Bearer other'}).set('Idempotency-Key','asset-key-123').send({prompt:'A cinematic prompt that is long',settings:{aspectRatio:'16:9',durationSeconds:4,quality:'draft'},inputAssets:[{assetId:up.body.assetId,purpose:'reference'}]}).expect(403);});});
